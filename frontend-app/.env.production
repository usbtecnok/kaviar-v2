# =========================================================
# KAVIAR FRONTEND - PRODUÇÃO (Vite)
# Arquivo: frontend-app/.env.production
# =========================================================

# Base URL da API em produção (Render)
# IMPORTANTE: produção usa /api/v1
VITE_API_BASE_URL=https://kaviar-v2.onrender.com/api/v1

# Compatibilidade (caso algum arquivo do projeto use outro nome)
# Deixar as duas apontando para o mesmo lugar evita quebra por variável diferente.
VITE_API_URL=https://kaviar-v2.onrender.com/api/v1

# Google Maps (se você ainda não configurou, pode deixar placeholder)
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here
